
export enum AuthStateConstants {
  LOGIN_STATE = "login",
  FORGOT_PASSWORD_STATE = "fogot-password"
}
export enum ResponseErrors {
  NETWORK_DISCONNECTED = "Could not connect to internet, check your network connections",
}
export enum ApiUrls {
  LOGOUT = '/logout'
}